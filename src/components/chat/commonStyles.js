import { StyleSheet } from 'react-native';

const commonStyles = StyleSheet.create({
  profileIcon: {
    width: 48,
    height: 48,
    borderWidth: 1,
    borderRadius: 24,
    marginRight: 16
  }
});

export default commonStyles;
